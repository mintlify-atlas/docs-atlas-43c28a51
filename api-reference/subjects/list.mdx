---
title: 'List subjects'
description: 'Retrieve all subjects belonging to the authenticated user.'
---

`GET /api/subjects`

Returns a list of all subjects owned by the authenticated user.

## Authentication

This endpoint requires a valid Bearer token in the `Authorization` header.

## Response

Returns an array of subject objects.

<ResponseField name="id" type="string" required>
  Unique identifier for the subject (UUID).
</ResponseField>

<ResponseField name="name" type="string" required>
  The name of the subject.
</ResponseField>

<ResponseField name="userId" type="string" required>
  The ID of the user who owns this subject.
</ResponseField>

## Error codes

| Status | Description |
| ------ | ----------- |
| `401 Unauthorized` | Missing or invalid Bearer token. |

<RequestExample>

```bash cURL
curl --request GET \
  --url https://api.scheduly.app/api/subjects \
  --header 'Authorization: Bearer <token>'
```

</RequestExample>

<ResponseExample>

```json 200
[
  {
    "id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "name": "Matemáticas",
    "userId": "f1e2d3c4-b5a6-7890-fedc-ba9876543210"
  },
  {
    "id": "b2c3d4e5-f6a7-8901-bcde-f12345678901",
    "name": "Física",
    "userId": "f1e2d3c4-b5a6-7890-fedc-ba9876543210"
  }
]
```

</ResponseExample>
